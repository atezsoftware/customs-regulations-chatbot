# Copy to `.env.migration`, provision through the production secret store, and
# chmod 600. This file is read only by the profile-gated regulatory_migration
# job; it must never be included in common `.env` or importer configuration.

POSTGRES_USER=
POSTGRES_PASSWORD=
