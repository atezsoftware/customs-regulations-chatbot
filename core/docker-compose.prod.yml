services:
  core:
    container_name: customs-core-prod
    restart: always
    # Intentionally no host port mapping. Backend reaches this service over
    # the private Docker network at ws://core:8000/ws/explore.
    deploy:
      resources:
        limits:
          memory: 2g
