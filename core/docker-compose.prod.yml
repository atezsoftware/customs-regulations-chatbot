services:
  core-api:
    container_name: customs-core-api-prod
    restart: always
    # Intentionally no host port mapping. Backend reaches this service over
    # the private Docker network at ws://core-api:8000/ws/explore.
    deploy:
      resources:
        limits:
          memory: 512m
