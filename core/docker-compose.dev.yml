services:
  core:
    container_name: customs-core-dev
    ports:
      - "127.0.0.1:${CORE_PORT:-8000}:8000"
    volumes:
      - ./src:/app/src
      - ../storage:/app/storage
    command: uv run uvicorn fs_explorer.server:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
